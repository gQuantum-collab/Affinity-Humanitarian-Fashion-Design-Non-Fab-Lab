rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /members/{wallet} {
      allow read: if true; // public member status (adjust if needed)
      allow write: if request.auth != null && request.auth.uid == wallet; // basic self-write (tighten later)
    }
  }
}
